工厂方法模式
通过实现计算器功能和简单工厂模式进行对比，看下相互之间的差异和优缺点
工厂方法模式相比于简单工厂模式最大的优点是，对于工厂类来说，更好的遵循了开放封闭原则。
但是把加减乘除的逻辑选择判断移到了客户端。从而导致如果想要增加一种计算类型的，除了要新增一个计算类型工厂类和实现类之外还需要修改客户端代码。

简单工厂模式如果要新增一种计算类型，需要修改工厂类，然后新增一个实现类即可。

彩蛋：利用反射可以解决需要进行分支判断的问题